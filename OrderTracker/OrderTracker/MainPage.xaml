<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="OrderTracker.MainPage"
             Title="Order Manager">

	<ContentPage.Content>
		<AbsoluteLayout x:Name="absLayout">
			<ScrollView Style="{StaticResource FormScroll}">
				<StackLayout x:Name="BaseStackLayout" Style="{StaticResource Form}"  IsEnabled="{Binding IsBusy, Converter={StaticResource Toggle}}" Orientation="Vertical" FlowDirection="LeftToRight" />
			</ScrollView>

			<ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" AbsoluteLayout.LayoutBounds="0.5,0.5,100,100" />
		</AbsoluteLayout>
	</ContentPage.Content>
</ContentPage>
